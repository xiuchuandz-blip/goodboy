{
  "app_dir": "artifacts/api-server",
  "build_command": "pnpm --filter @workspace/landing run build && pnpm --filter @workspace/api-server run build",
  "start_command": "node --enable-source-maps ./dist/index.mjs"
}
